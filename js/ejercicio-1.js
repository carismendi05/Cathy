// Crear un vector vacío llamado "colores". Luego agrega 3 colores diferentes y muestra el vector. 

let colores = []

colores.push("Plateado", "Ocre", "Negro");

for(let i = 0; i < colores.length; i++) {
    console.log (colores[i]);
}
